#ifndef CBLOG_BUILDER_H
#define CBLOG_BUILDER_H

#include "cblog.h"

/* Build the entire static site from the current project directory. */
int builder_build(const char *project_dir);

#endif /* CBLOG_BUILDER_H */
